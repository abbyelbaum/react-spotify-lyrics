fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
python-dotenv>=1.0
itsdangerous>=2.2
lyricsgenius>=3.0
pydantic>=2.9
# Only used when DATABASE_URL is set (production). Local dev with SQLite
# doesn't import this — install is still cheap so we keep it required.
psycopg[binary]>=3.1
